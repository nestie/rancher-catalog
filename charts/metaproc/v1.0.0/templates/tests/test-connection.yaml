apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "metaproc.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "metaproc.name" . }}
    helm.sh/chart: {{ include "metaproc.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "metaproc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
